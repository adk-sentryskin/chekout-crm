# =============================================================================
# CRM Microservice Configuration
# =============================================================================

# Database Configuration
# Local dev: run Cloud SQL Proxy and connect via localhost (secure tunnel)
DB_DSN=postgresql://user:password@127.0.0.1:5432/database

# Cloud SQL Proxy - Instance Connection Name
# Local dev: start proxy with: cloud-sql-proxy <project>:<region>:<instance> --port 5432
# Cloud Run: set this to enable Unix socket (auto-managed by --add-cloudsql-instances)
# INSTANCE_CONNECTION_NAME=your-project:your-region:your-instance

# Environment
ENVIRONMENT=development
DEBUG=true
LOG_LEVEL=INFO

# CRM Encryption (Required - must be exactly 32 characters)
CRM_ENCRYPTION_KEY=your-32-character-encryption-key-here

# Service Port
PORT=8001

# Optional: API Key for service-to-service authentication
# API_KEY=your-api-key-for-internal-services

# Optional: CORS (only if CRM service is accessed directly from frontend)
CORS_ALLOWED_ORIGINS=*
