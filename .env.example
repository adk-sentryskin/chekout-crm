# =============================================================================
# CRM Microservice Configuration
# =============================================================================

# Database Configuration
DB_DSN=postgresql://user:password@localhost:5432/database?options=-c%20search_path=crm

# Environment
ENVIRONMENT=development
DEBUG=true
LOG_LEVEL=INFO

# CRM Encryption (Required - must be exactly 32 characters)
CRM_ENCRYPTION_KEY=your-32-character-encryption-key-here

# Service Port
PORT=8001

# Optional: API Key for service-to-service authentication
# API_KEY=your-api-key-for-internal-services

# Optional: CORS (only if CRM service is accessed directly from frontend)
CORS_ALLOWED_ORIGINS=*
